Olá
Deixei os scripts na pasta raiz do projeto fazendo referencia ao banco e tabelas utilizadas na API.
Para complilar o projeto é bem simples, é um projeto Maven criado com Spring Boot initializer.
Acredito que como as dependencias estão mapeadas, quando o projeto for compilado a propria IDE adicionara as dependencias.
Construi o projeto utilizando o intellij, caso rode pelo eclipse sera necessario apontar o caminho para a JDK 11.